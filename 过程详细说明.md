# GGEUR（CPSD 跨域情感）实验过程详细说明

本说明对应一次实际运行：

- 命令：`python federatedscope/main.py --cfg scripts/example_configs/ggeur_rnn_sentiment.yaml`
- 输出目录：`exp/ggeur_rnn_sentiment/ggeur_cpsd_lstm/sub_exp_20260120172157`
- 关键日志：`exp/ggeur_rnn_sentiment/ggeur_cpsd_lstm/sub_exp_20260120172157/exp_print.log`
- 实际生效配置：`exp/ggeur_rnn_sentiment/ggeur_cpsd_lstm/sub_exp_20260120172157/config.yaml`

---

## 1. 实验目标与整体流程概览

### 1.1 任务与数据

- 任务：二分类情感分析（positive/negative）
- 数据集：CPSD（Cross-domain Polarity Sentiment Dataset）
  - 域（domains）：`Sentiment140`、`Yelp`、`IMDb`
  - 每个域内部再划分为多个客户端（clients）

### 1.2 联邦学习设置（本次运行）

来自 `config.yaml` 与日志汇总：

- FL 方法：`federate.method = ggeur`
- 运行模式：`federate.mode = standalone`
- 客户端数量：`federate.client_num = 30`（3 个域 × 每域 10 个客户端）
- 每轮采样：`federate.sample_client_num = 30`（即每轮全量客户端参与）
- 总轮数：`federate.total_round_num = 50`
  - 注意：GGEUR 在本实现里会用 `statistics_round=0` 做统计与增强准备，因此**Round 0 用于统计与增强**，之后进入 FedAvg 训练，训练轮从 Round 1 开始，到 Round 49 结束（共 49 轮训练 + 1 轮统计 = 50 state）。

### 1.3 整体流程（高层）

本实验采用“冻结特征提取器 + 训练轻量分类器”的两阶段范式：

1. **数据加载与划分（CPSD）**：三域数据加载，域内按 Dirichlet 标签偏斜划分为 10 个客户端。
2. **Round 0 统计阶段（GGEUR）**：
   - 客户端用 BERT 从文本提取句向量特征（embedding）
   - 计算每类的均值（prototype）与协方差（covariance）
   - 上传到服务器
   - 服务器聚合得到全局协方差与全局原型，并为每个客户端准备“来自其他客户端的原型集合”
3. **特征级数据增强（GGEUR）**：
   - 客户端基于“全局协方差 + 本地样本/跨客户端原型”进行高斯采样生成特征
   - 每类采样后裁剪/抽样到固定规模（`target_size_per_class`），形成平衡的增强训练集
4. **Round 1..49 联邦训练（FedAvg）**：
   - 训练模型：轻量 LSTM 分类器（只在特征上训练，不回传/更新 BERT）
   - 每轮：客户端本地训练 → 上传模型参数 → 服务器按样本数加权聚合（FedAvg）→ 服务器在三域测试集上评估

---

## 2. 模型架构（本次运行）

本次运行的模型由两部分组成：

1. **冻结特征提取器：BERT（只做前向提特征）**
2. **可训练分类器：LSTM（在特征空间上训练）**

### 2.1 冻结特征提取器：BERT（Sentence Embedding）

配置（`config.yaml -> ggeur.*`）：

- `ggeur.feature_extractor = bert`
- `ggeur.bert_model_path = pretrained_models/nlptown_bert_base_multilingual_uncased_senti`
- `ggeur.bert_max_length = 128`
- `ggeur.bert_pooling = cls`
- `ggeur.embedding_dim = 768`
- `ggeur.bert_local_files_only = true`（仅使用本地模型文件）
- `ggeur.use_feature_cache = true`（特征落盘缓存）

行为要点（见 `federatedscope/contrib/worker/ggeur_client.py` 与 `federatedscope/contrib/worker/ggeur_server.py`）：

- BERT 仅用于提取 `last_hidden_state`，不参与训练（`model.eval()` + `torch.no_grad()`）。
- pooling 方式为 `cls`：取 `hidden[:, 0, :]` 作为句向量（768 维）。

### 2.2 可训练分类器：GGEUR LSTM 文本分类器

本次实际生效模型类型为：`model.type = ggeur_lstm`（见输出目录 `config.yaml`）。

结构定义位于：`federatedscope/contrib/model/ggeur_text_rnn.py`，核心行为：

- 输入：特征 `x`，形状为 `(B, D)` 或 `(B, T, D)`
  - 本实验由 BERT pooling 得到句向量，因此通常为 `(B, 768)`
  - 代码会将 `(B, D)` 自动扩展为 `(B, 1, D)`，即序列长度 `T=1`
- 主干：`nn.LSTM(input_size=768, hidden_size=256, num_layers=1, batch_first=True)`
- 取最后时间步输出：`last = out[:, -1, :]`（形状 `(B, 256)`）
- Dropout：`dropout=0.1`
- 分类头：`Linear(256 -> 2)` 输出 logits

本次关键超参（`config.yaml -> model.*`）：

- `model.in_channels = 768`
- `model.hidden = 256`
- `model.layer = 1`
- `model.dropout = 0.1`
- `model.num_classes = 2`

---

## 3. 特征提取过程（BERT -> Embedding）

### 3.1 在客户端侧提取训练特征（Round 0）

触发时机：Round 0（`statistics_round=0`）进入统计阶段。

代码路径：

- 客户端统计与特征提取：`federatedscope/contrib/worker/ggeur_client.py`
  - `_load_bert_model()`
  - `_extract_bert_features(...)`
  - `_compute_local_statistics()`
  - `_upload_local_statistics()`

具体流程：

1. **加载 BERT tokenizer 与 model**
   - 使用 HuggingFace `transformers.AutoTokenizer/AutoModel`
   - 若同一进程内多个客户端共用同一 BERT（相同路径、设备、local_only 等），会复用 `_SHARED_BERT_EXTRACTORS`，避免“每个客户端加载一份大模型”。
2. **特征缓存命中优先**
   - 若启用 `ggeur.use_feature_cache=true`，会在默认目录 `data/text_feature_cache` 下生成 `.npz` 缓存文件（可配置 `ggeur.feature_cache_dir` 覆盖）。
   - 每条样本使用数据集稳定 id（`CPSDTextDataset.get_id()`）作为缓存 key；命中则直接取缓存 embedding，跳过 BERT 前向。
3. **批量 tokenization 与 BERT 前向**
   - `padding=True, truncation=True, max_length=128, return_tensors='pt'`
   - 前向输出 `outputs.last_hidden_state`，形状 `(B, T, 768)`
4. **Pooling 得到句向量**
   - `bert_pooling=cls`：取 `hidden[:, 0, :]` 得到 `(B, 768)`
   - 转为 `float32 numpy`，按类别写入 `self.local_features[label]`
5. **缓存落盘**
   - 只要本轮有新增 embedding，会将 `{sample_id -> embedding}` 保存为 `.npz`

### 3.2 在服务器侧提取测试特征（用于每轮评估）

服务器评估需要统一的测试集特征，因此会在首次评估前加载并缓存三域测试特征：

- 代码：`federatedscope/contrib/worker/ggeur_server.py`
  - `_load_test_data_and_features()` 在 CPSD 分支中执行 BERT 特征抽取
  - `_get_cpsd_test_cache_path(...)` 生成包含采样参数的 cache 文件名（避免复用旧缓存导致评估偏差）

关键点：

- 服务器同样使用 `bert_pooling=cls`、`bert_max_length=128`、`bert_batch_size=32`。
- 测试特征缓存会包含 `max_test_samples_per_domain` 与 `seed`，保证当“每域采样上限/随机种子变化”时不会误用旧缓存。

---

## 4. 数据增强过程（GGEUR 特征级增强）

本实验的数据增强是**在特征空间进行**，不是对原始文本做同义词替换等 NLP 增强。

### 4.1 Round 0：客户端统计上传

客户端对每个类别 `c` 计算：

- 样本数：`n_{k,c}`
- 类均值（prototype）：`μ_{k,c} = mean(X_{k,c})`
- 类协方差：`Σ_{k,c} = (1/n) * (X-μ)^T (X-μ)`

上传内容（见 `_upload_local_statistics()`）：

- `means`：各类均值
- `covs`：各类协方差
- `counts`：各类样本数
- `prototypes`：原型（这里等同于均值）

### 4.2 服务器聚合：全局协方差与全局原型

服务器收到所有客户端统计后（30/30）：

1. **聚合全局协方差（parallel axis theorem）**  
   对每个类别 `c`：

   - 全局均值：`μ_c = (Σ_k n_{k,c} μ_{k,c}) / (Σ_k n_{k,c})`
   - 全局协方差：
     ```
     Σ_c = ( Σ_k n_{k,c} Σ_{k,c}  +  Σ_k n_{k,c} (μ_{k,c}-μ_c)(μ_{k,c}-μ_c)^T ) / (Σ_k n_{k,c})
     ```

2. **计算全局原型（加权均值）**：`global_prototypes[c] = μ_c`
3. **为每个客户端准备“其他客户端原型集合”**
   - 对客户端 k，`other_prototypes[k][c]` 收集所有 `k'!=k` 的 `μ_{k',c}` 列表

然后服务器向每个客户端广播 `global_covariances` 消息，包含：

- `cov_matrices`：全局协方差 `Σ_c`
- `other_prototypes`：该客户端可用的跨客户端原型列表
- `global_prototypes`：全局原型（本次不启用 feature alignment 时主要用于兼容）

### 4.3 客户端增强：高斯采样 + 目标规模裁剪

客户端收到 `global_covariances` 后执行 `_perform_augmentation()`：

配置（`config.yaml -> ggeur.*`）：

- `ggeur.num_generated_per_sample = 50`
- `ggeur.num_generated_per_prototype = 50`
- `ggeur.target_size_per_class = 50`
- `ggeur.use_cross_client_prototypes = true`

对每个类别 `c`：

1. **保留本地原始特征**（始终包含）
2. **基于本地样本做扩增**  
   对该类的每个原始特征向量 `x`，从 `N(x, Σ_c)` 采样 `50` 个增强特征
3. **基于跨客户端原型做扩增**  
   对 `other_prototypes[c]` 中每个原型向量 `p`，从 `N(p, Σ_c)` 采样 `50` 个增强特征
4. **合并后按目标规模抽样**
   - 若合并样本数 ≥ `target_size_per_class(50)`，则无放回随机抽取 50 个
   - 否则保留全部

最终增强数据集由 `AugmentedFeatureDataset(augmented_features, augmented_labels)` 构造，并以 `batch_size=32` 进行训练。

本次运行的日志显示每个客户端增强后均为：

- `Augmented data - 100 samples, 2 classes`

即二分类场景下每类 50，总计 100 条特征样本；因此每轮聚合的样本总数为 `30 * 100 = 3000`（日志中多轮均显示 `total samples: 3000`）。

### 4.4 数值稳定性与高维采样实现要点

特征维度为 768（高维），实现中做了若干稳定性处理（见 `_nearest_pos_def()` 与 `_generate_samples()`）：

- 对高维协方差矩阵优先尝试 Cholesky 分解 + jitter；失败则退化为对角近似采样
- 对较低维度会做特征值分解与修正，确保协方差半正定/正定

---

## 5. 训练过程（Round 1..49：FedAvg 训练 LSTM 分类器）

### 5.1 本地训练（客户端）

客户端在 Round 1 开始进入训练阶段（日志为 `Round X - Training on augmented data`），每轮执行 `_train_on_augmented_data()`：

- 优化器：`Adam(lr=0.001)`（来自 `train.optimizer.lr`）
- 损失：`CrossEntropyLoss`
- 本地训练轮数：`train.local_update_steps = 1`
  - 注意：实现里该参数作为 `local_epochs` 使用，即**每轮对增强数据集训练 1 个 epoch**
- 输入：增强后的特征 `features`（float tensor，形状 `(B, 768)`）
- 输出：LSTM 分类器 logits（形状 `(B, 2)`）

每个客户端训练完成后上传：

- `(sample_size, model_state_dict)`：`sample_size` 为本轮本地训练样本数（本次为 100）

### 5.2 服务器聚合（FedAvg）

服务器端收到所有客户端模型后进行聚合（`_perform_fedavg()`）：

- 使用样本数加权的 FedAvg，对每个参数张量做加权平均
- 更新全局 LSTM 分类器参数

### 5.3 每轮评估（三域测试集）

每轮聚合后服务器执行 `_evaluate_on_test_sets()`：

- 测试特征：服务器侧用同一 BERT 对三域测试集抽取（并缓存）
- 评估方式：将 `(N, 768)` 测试特征送入全局 LSTM 分类器，计算 accuracy
- 日志格式：`Server: Round R MLP Test Accuracy - Sentiment140: ..., Yelp: ..., IMDb: ..., average: ...`

### 5.4 本次运行的最终与最优结果（来自日志末尾）

日志汇总（`exp_print.log` 末尾）：

- `Sentiment140`: final=0.80, best=0.85
- `Yelp`: final=1.00, best=1.00
- `IMDb`: final=0.80, best=0.80
- `average`: final=0.8667, best=0.8833

其中最优平均准确率为：`Classifier Best Average Accuracy: 0.8833`

---

## 6. 关键配置一览（便于复现实验）

### 6.1 数据与划分

- `data.type = cpsd`
- `data.root = data/CPSD`
- `data.splits = [0.9, 0.1, 0.0]`（每域 train 内部再切 train/val；test 用官方 test）
- `data.dirichlet_alpha = 0.5`（域内客户端标签偏斜）
- `data.args[0].max_train_samples_per_domain = 20000`
- `data.args[0].max_test_samples_per_domain = 5000`
- `data.args[0].seed = 42`
- `data.args[0].use_cache = true`

### 6.2 特征提取（BERT）

- `ggeur.feature_extractor = bert`
- `ggeur.bert_model_path = pretrained_models/nlptown_bert_base_multilingual_uncased_senti`
- `ggeur.bert_max_length = 128`
- `ggeur.bert_pooling = cls`
- `ggeur.bert_batch_size = 32`
- `ggeur.embedding_dim = 768`
- `ggeur.use_feature_cache = true`

### 6.3 特征级增强（GGEUR）

- `ggeur.statistics_round = 0`
- `ggeur.use_cross_client_prototypes = true`
- `ggeur.num_generated_per_sample = 50`
- `ggeur.num_generated_per_prototype = 50`
- `ggeur.target_size_per_class = 50`

### 6.4 分类器与训练

- `model.type = ggeur_lstm`
- `model.in_channels = 768`
- `model.hidden = 256`
- `model.layer = 1`
- `model.dropout = 0.1`
- `model.num_classes = 2`
- `train.local_update_steps = 1`
- `train.optimizer.type = Adam`
- `train.optimizer.lr = 0.001`
- `dataloader.batch_size = 32`

---

## 7. 多配置对比（GGEUR vs FedAvg，RNN vs LSTM）

本节对比 4 次运行（均为 CPSD + BERT 特征 + RNN/LSTM 分类器；联邦聚合规则为 FedAvg）：

**RNN（`model.type = ggeur_rnn`）**

- GGEUR-RNN（增强开启）
  - 命令：`python federatedscope/main.py --cfg scripts/example_configs/ggeur_rnn_sentiment.yaml`
  - 日志：`exp/ggeur_rnn_sentiment/ggeur_cpsd_lstm/sub_exp_20260121144900/exp_print.log`
  - 配置：`exp/ggeur_rnn_sentiment/ggeur_cpsd_lstm/sub_exp_20260121144900/config.yaml`
- FedAvg-RNN（无增强 baseline）
  - 命令：`python federatedscope/main.py --cfg scripts/example_configs/fedavg_rnn_sentiment.yaml`
  - 日志：`exp/fedavg_rnn_sentiment/fedavg_cpsd_lstm_no_augmentation/sub_exp_20260126112305/exp_print.log`
  - 配置：`exp/fedavg_rnn_sentiment/fedavg_cpsd_lstm_no_augmentation/sub_exp_20260126112305/config.yaml`

**LSTM（`model.type = ggeur_lstm`）**

- GGEUR-LSTM（增强开启）
  - 命令：`python federatedscope/main.py --cfg scripts/example_configs/ggeur_rnn_sentiment.yaml`
  - 日志：`exp/ggeur_rnn_sentiment/ggeur_cpsd_lstm/sub_exp_20260120172157/exp_print.log`
  - 配置：`exp/ggeur_rnn_sentiment/ggeur_cpsd_lstm/sub_exp_20260120172157/config.yaml`
- FedAvg-LSTM（无增强 baseline）
  - 命令：`python federatedscope/main.py --cfg scripts/example_configs/fedavg_rnn_sentiment.yaml`
  - 日志：`exp/fedavg_rnn_sentiment/fedavg_cpsd_lstm_no_augmentation/sub_exp_20260121113120/exp_print.log`
  - 配置：`exp/fedavg_rnn_sentiment/fedavg_cpsd_lstm_no_augmentation/sub_exp_20260121113120/config.yaml`

> 说明：以上对比以各自输出目录中的 `config.yaml` 为准（这是运行时最终生效配置，避免示例 cfg 文件后续被修改导致不一致）。

### 7.1 配置差异总览（关键字段）

| 对比项 | GGEUR-RNN | FedAvg-RNN（NoAug） | GGEUR-LSTM | FedAvg-LSTM（NoAug） |
|---|---|---|---|---|
| `model.type` | `ggeur_rnn` | `ggeur_rnn` | `ggeur_lstm` | `ggeur_lstm` |
| BERT 特征提取 | `feature_extractor=bert`，`pooling=cls`，`max_length=128` | 同左 | 同左 | 同左 |
| `ggeur.bert_use_pretrained_weights` | `true` | `false` | （配置未显式写入；代码默认 `true`） | `false` |
| 特征缓存 | `ggeur.use_feature_cache=true` | `true` | `true` | `true` |
| `ggeur.num_generated_per_sample` | `50` | `0` | `50` | `0` |
| `ggeur.num_generated_per_prototype` | `50` | `0` | `50` | `0` |
| `ggeur.target_size_per_class` | `50` | `0` | `50` | `0` |
| `ggeur.use_cross_client_prototypes` | `true` | `false` | `true` | `false` |
| `data.dirichlet_alpha` | `1.0` | `1.0` | `0.5` | `1.0` |
| 每轮聚合样本数（日志） | `3000` | `54000` | `3000` | `54000` |
| `sys_avg/fl_end_time_minutes` | `46.2846` | `7.1096` | `46.9018` | `9.8326` |

结论（先给出直观差异）：

1. **GGEUR vs FedAvg baseline 的核心差异不止“是否增强”**：baseline 还额外将 `bert_use_pretrained_weights=false`，即 BERT 以随机初始化权重提取特征，导致分类性能接近随机猜测（≈0.5）。
2. **GGEUR 训练样本规模被强制“按类平衡 + 固定上限”**：每客户端每轮 2 类各 50，共 100；全局每轮 3000。baseline 使用原始样本，单客户端样本数从几百到几千不等，全局每轮 54000。
3. **LSTM 的 GGEUR 与 RNN 的 GGEUR 之间还存在 `dirichlet_alpha` 与评估缓存规模差异**，不能把指标差异完全归因于 RNN/LSTM 单一因素（见 7.5 与 7.6 说明）。

### 7.2 模型架构对比（RNN vs LSTM；GGEUR vs baseline）

#### 7.2.1 共性：冻结 BERT + 轻量分类器

四个配置的“可训练模型”都是 `ggeur_text_rnn.py` 中的轻量序列分类器，输入为 BERT 句向量：

- BERT 输出：`embedding_dim=768`
- pooling：`cls`（取 `[CLS]` 向量）
- 分类器输入：形状通常为 `(B, 768)`
  - 由于实现中若输入是 `(B, D)` 会自动扩成 `(B, 1, D)`，即序列长度 `T=1`

因此，这里的 RNN/LSTM **不是在建模“词序列”**，而是在做一个“对句向量的非线性映射 + 线性分类”。如果希望 RNN/LSTM 发挥序列建模优势，需要改成输入 `(B, T, D)`（例如使用 token-level 表示而非 `cls` pooling）。

#### 7.2.2 RNN vs LSTM（参数量与表达能力）

在 `input_size=768, hidden_size=256, num_layers=1` 下：

- `ggeur_rnn`：`nn.RNN`（tanh）
  - RNN 主体参数量约：`256*768 + 256*256 + 2*256 ≈ 262,656`
  - 加上分类头 `Linear(256->2)` 约 `514`，总计约 **263k** 参数
- `ggeur_lstm`：`nn.LSTM`（4 门控）
  - LSTM 主体参数量约：`4*256*768 + 4*256*256 + 2*(4*256) ≈ 1,050,624`
  - 加上分类头 `≈514`，总计约 **1.05M** 参数

在本实验 `T=1` 的前提下，LSTM 的门控结构更多体现在“更复杂的输入-隐层映射”，而不是长程记忆；因此 RNN/LSTM 的效果差异可能不如在真正序列输入时显著。

#### 7.2.3 GGEUR vs FedAvg baseline：架构相同，数据/特征管线不同

GGEUR 与 baseline 使用**同一套分类器结构**（同样的 `model.type`、`hidden/layer/dropout`），差异主要来自：

1. BERT 特征是否来自预训练权重（`bert_use_pretrained_weights`）
2. 是否执行特征级数据增强（`num_generated_*`、`target_size_per_class`、`use_cross_client_prototypes`）
3. 训练时每客户端样本规模/类别分布，从而改变 FedAvg 的加权与收敛行为

### 7.3 特征提取对比（BERT）

#### 7.3.1 共同点

四组实验均在 Round 0 统计阶段提取训练特征、在服务器评估阶段提取测试特征：

- tokenizer：与 `bert_model_path` 对应（`bert_tokenizer_path` 为空时默认同路径）
- `bert_max_length=128`，`bert_batch_size=32`
- pooling：`cls`（取 `hidden[:, 0, :]`）
- 训练阶段不更新 BERT（只做前向提特征）

#### 7.3.2 关键差异：是否使用预训练 BERT 权重

- GGEUR-RNN：`bert_use_pretrained_weights=true`（特征具备语义/情感先验）
- FedAvg-RNN：`bert_use_pretrained_weights=false`（随机初始化 BERT；特征语义性弱，效果接近随机）
- GGEUR-LSTM：该 run 的 `config.yaml` 未显式写入该字段（早期版本配置缺失），实现默认 `true`
- FedAvg-LSTM：`bert_use_pretrained_weights=false`

> 如果希望“只对比是否增强”，建议将 baseline 的 `bert_use_pretrained_weights` 也设为 `true`，仅关闭增强相关参数（见 7.4）。

### 7.4 数据增强对比（特征级）

#### 7.4.1 GGEUR：基于全局协方差 + 跨客户端原型的高斯采样

增强开启（GGEUR-RNN / GGEUR-LSTM）时：

1. 客户端 Round 0 提取 BERT 特征后，按类计算本地 `(μ_{k,c}, Σ_{k,c}, n_{k,c})` 上传
2. 服务器聚合得到全局协方差 `Σ_c` 与全局原型 `μ_c`，并为每个客户端准备 `other_prototypes[k][c]`
3. 客户端对每一类 `c` 生成特征：
   - 以本地每个样本特征 `x` 为均值，从 `N(x, Σ_c)` 采样 `num_generated_per_sample` 个
   - 以其他客户端原型 `p` 为均值，从 `N(p, Σ_c)` 采样 `num_generated_per_prototype` 个
   - 与原始特征合并后，若数量 ≥ `target_size_per_class`，则无放回随机抽取 `target_size_per_class`

本次两次 GGEUR run 的关键超参相同：

- `num_generated_per_sample=50`
- `num_generated_per_prototype=50`
- `target_size_per_class=50`
- `use_cross_client_prototypes=true`

因此二分类下每客户端最终训练集规模为 **`50 * 2 = 100`**，日志也显示 `Augmented data - 100 samples, 2 classes`；每轮 FedAvg 聚合样本数为 **`30 * 100 = 3000`**。

#### 7.4.2 FedAvg baseline（NoAug）：直接使用原始特征，不做采样增强/重平衡

baseline（FedAvg-RNN / FedAvg-LSTM）将增强相关参数置零：

- `num_generated_per_sample=0`
- `num_generated_per_prototype=0`
- `target_size_per_class=0`
- `use_cross_client_prototypes=false`

客户端收到全局协方差信息后会进入 “No augmentation mode”，直接用本地原始特征构建训练集（日志为 `Original data - XXX samples, 2 classes`）。

- 本次 baseline 每轮聚合样本数为 **54000**（来自日志 `total samples: 54000`），反映的是 30 个客户端原始训练样本总和。
- 由于各客户端样本数不均，FedAvg 的加权（sample_size）会让大客户端对全局更新影响更大；而 GGEUR 因每客户端固定 100 样本，使得加权更接近“按客户端均匀”。

### 7.5 训练过程对比（联邦轮次、优化、评估）

#### 7.5.1 联邦轮次结构

四组实验均为：

- `total_round_num=50`
- Round 0：统计/特征提取阶段（GGEUR 管线需要）
- Round 1..49：联邦训练阶段（FedAvg 聚合）

其中 Round 0 的差异在于：

- GGEUR：统计上传 + 增强采样 + 构建增强训练集
- baseline：统计上传后虽然也会触发“构建训练集”，但为 NoAug，训练集=原始特征

#### 7.5.2 本地训练与聚合

四组实验的训练超参一致：

- `train.local_update_steps=1`（实现中等价于每轮本地训练 1 个 epoch）
- `optimizer=Adam(lr=0.001)`
- `loss=CrossEntropyLoss`
- `batch_size=32`

主要差异来自训练数据：

- GGEUR：每客户端每轮 100 条“按类平衡”的特征样本（聚合总样本 3000）
- baseline：每客户端每轮使用全部原始特征（聚合总样本 54000，且客户端间不均衡）

#### 7.5.3 评估（服务器侧）与可比性注意事项

服务器评估逻辑为：对三域测试集提取 BERT 特征（可缓存），再喂给全局分类器，按域计算准确率并求平均。

- RNN 对比（2026-01-21 / 2026-01-26 的两次 run）测试特征规模为：Sentiment140=359，Yelp=5000，IMDb=5000（日志为 `Extracted/Loaded ... CPSD test features`）。
- **GGEUR-LSTM 的旧 run（2026-01-20）日志显示从缓存读取的测试特征仅 20/域**（`Loaded 20 cached CPSD test features ...`），该评估规模与后续 runs 不一致，可能导致指标偏高或波动更大；进行严格对比时建议清理旧缓存或使用相同缓存 key 重新抽取。

### 7.6 结果对比（最终/最优；来自日志末尾）

| 指标 | GGEUR-RNN | FedAvg-RNN（NoAug） | GGEUR-LSTM | FedAvg-LSTM（NoAug） |
|---|---:|---:|---:|---:|
| Sentiment140（final / best） | 0.7994 / 0.8440 | 0.4930 / 0.5404 | 0.8000 / 0.8500 | 0.4930 / 0.5265 |
| Yelp（final / best） | 0.8984 / 0.9036 | 0.5000 / 0.5116 | 1.0000 / 1.0000 | 0.5000 / 0.5130 |
| IMDb（final / best） | 0.8152 / 0.8250 | 0.5000 / 0.5170 | 0.8000 / 0.8000 | 0.5000 / 0.5152 |
| average（final / best） | 0.8377 / 0.8557 | 0.4977 / 0.5165 | 0.8667 / 0.8833 | 0.4977 / 0.5074 |
| `sys_avg/fl_end_time_minutes` | 46.2846 | 7.1096 | 46.9018 | 9.8326 |

对结果的解释（结合配置差异）：

- baseline 两组（RNN/LSTM）准确率接近 0.5，主要原因是 `bert_use_pretrained_weights=false` 导致 BERT 特征近似随机；这比“是否增强”对结果的影响更大。
- GGEUR 两组使用预训练 BERT（RNN 明确为 `true`；LSTM run 默认为 `true`）且开启特征增强，准确率显著提升。
- 直接比较 “GGEUR-RNN vs GGEUR-LSTM” 还受到 `dirichlet_alpha`（1.0 vs 0.5）与 LSTM run 的测试缓存规模（20/域）影响，建议在相同 `dirichlet_alpha`、相同测试缓存规模下复现实验后再做结论。
