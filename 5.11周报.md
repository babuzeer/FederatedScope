# 5.11周报

## 时间范围

2026 年 4 月 28 日至 2026 年 5 月 12 日

## 本阶段工作概述

本阶段的工作主要围绕 `FederatedScope` 框架下 `GGEUR` 方法的多数据集扩展、实验配置补充、训练流程排查与实验支撑脚本完善展开。重点推进了 `DomainNet` 数据集接入、`DomainNet` 多组 baseline 实验配置搭建、`PromptFL` 相关功能扩展，以及 `FedOpt` 训练异常问题的定位与修复。

## 主要工作内容

### 1. 完成 DomainNet 数据集接入与处理流程梳理

本阶段新增了 `DomainNet` 数据集在项目中的读取、划分和测试支持，补充了数据集定义文件：

- `federatedscope/cv/dataset/domainnet.py`

同时，在以下模块中完成了 `DomainNet` 的联邦数据加载、评测和缓存逻辑接入：

- `federatedscope/contrib/data/ggeur_data.py`
- `federatedscope/contrib/worker/ggeur_server.py`

此外，新增说明文档：

- `docs/DomainNet_完整说明.md`

该文档对当前项目中 `DomainNet` 的目录结构、类别组织方式、训练测试划分策略以及 LDS 条件下的 client 划分逻辑进行了系统整理。当前实现采用 4 个 domain，类别默认取所选 domain 的并集，图像统一缩放到 `224x224`，使用 `CLIP` 风格归一化，不额外进行数据增强；数据划分采用每个 domain 内部 `7:3` 的 train/test 切分方式，测试口径为同域测试而非跨域测试。

### 2. 补齐 DomainNet 上的多组对比实验配置

围绕 `DomainNet 4 domains` 场景，补充了三类 backbone 分支下的对比实验配置：

- `scripts/example_configs/ggeur_baseline_vit_domainnet/`
- `scripts/example_configs/ggeur_baseline_cnn_domainnet/`
- `scripts/example_configs/ggeur_baseline_mlp_domainnet/`

其中覆盖了以下实验方法：

- `FedAvg`
- `FedProx`
- `FedProto`
- `FedOpt`
- `MOON`
- `GGEUR + FedAvg`

这部分工作使 `DomainNet` 上的实验矩阵基本完整，能够支持后续在不同 backbone 与不同联邦优化策略之间进行系统对比。

### 3. 新增 DomainNet 批量实验运行脚本

为提高实验执行效率，新增批量运行脚本：

- `scripts/run_domainnet_all.sh`

该脚本支持按 `vit`、`cnn`、`mlp` 或 `all` 模式顺序运行对应配置文件，并自动输出日志、生成运行计划和汇总信息，便于后续大规模实验管理与结果整理。

### 4. 推进 GGEUR 与 PromptFL 的结合实验

本阶段继续完善 `GGEUR + PromptFL` 相关实现，对以下模块进行了扩展和调整：

- `federatedscope/contrib/model/ggeur_prompt.py`
- `federatedscope/contrib/worker/ggeur_client.py`
- `federatedscope/contrib/worker/ggeur_server.py`
- `federatedscope/core/configs/cfg_ggeur.py`

同时更新了 PromptFL 对应配置文件：

- `scripts/example_configs/ggeur_promptfl/ggeur_promptfl_clip.yaml`
- `scripts/example_configs/ggeur_promptfl/ggeur_promptfl_clip_pacs.yaml`

并新增结果绘图脚本：

- `scripts/plot_promptfl_accuracy.py`

这部分工作主要用于推进 `GGEUR` 与软提示学习结合的实验设计与结果分析，为后续继续开展 PromptFL 相关实验提供支撑。

### 5. 完善 Mixer 分支在 OfficeHome 和 PACS 上的 baseline 配置

对 `Mixer` 相关 baseline 配置进行了统一调整，涉及以下目录：

- `scripts/example_configs/ggeur_baseline_mixer/`
- `scripts/example_configs/ggeur_baseline_mixer_pacs/`

本次调整覆盖了 `FedAvg`、`FedProx`、`FedProto`、`FedOpt`、`MOON` 以及带/不带 `GGEUR` 的实验配置，主要目的是统一 `OfficeHome` 和 `PACS` 数据集上的 `Mixer` 分支实验设置，便于后续做横向对照分析。

### 6. 排查并修复 FedOpt 在 GGEUR 流程中的实现问题

在 `DomainNet + ViT` 的 `FedOpt` 实验过程中，发现模型准确率接近 0。对配置和训练逻辑进行排查后，定位到：

- `federatedscope/contrib/worker/ggeur_server.py`

中的 `FedOpt` server-side 更新实现存在伪梯度方向错误问题。原实现会将全局模型向错误方向更新，导致训练效果严重异常。该问题已完成修复，使当前实现与框架原版 `FedOptAggregator` 的更新逻辑保持一致。

这一修复为后续重新运行 `FedOpt` 实验并进一步调节 server learning rate 等超参数打下了基础，也提高了相关实验结果的可信度。

## 当前阶段成果

截至目前，本阶段工作已完成以下成果：

- 完成 `DomainNet` 数据集在项目中的接入与说明整理
- 补齐 `DomainNet` 上 `vit/cnn/mlp` 三类分支的 baseline 配置
- 新增批量实验运行脚本，提升实验执行效率
- 推进 `GGEUR + PromptFL` 功能扩展与实验准备
- 完善 `Mixer` 分支在 `OfficeHome` 和 `PACS` 上的 baseline 配置
- 定位并修复 `FedOpt` 在 `GGEUR` 训练流程中的关键实现问题

## 下一步计划

下一阶段将重点推进以下工作：

- 重新运行并验证 `DomainNet + ViT` 下的 `FedOpt` 实验结果
- 继续完成 `DomainNet` 不同方法之间的性能对比
- 整理 `PromptFL` 相关实验结果并分析其有效性
- 进一步统一不同 backbone 和不同数据集下的实验配置口径
- 结合实验结果完善文档与阶段性总结材料
